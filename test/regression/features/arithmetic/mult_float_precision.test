# We check the most significant digits in the output only due to differences in the
# printed output between haskell and python in the least significant digits
blip -c mult_float_precision.py && python3 mult_float_precision.pyc
<<<
>>> /7.986/
>>>2
>>>=0
